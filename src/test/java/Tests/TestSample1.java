package Tests;

import org.openqa.selenium.JavascriptExecutor;
import org.openqa.selenium.WebDriver;

import utilityMethod.BaseDriver;

public class TestSample1 extends BaseDriver {

	/*
	 * TestSample1(WebDriver driver) { super(driver); // TODO Auto-generated
	 * constructor stub }
	 * 
	 * 
	 * public static void main(String[] args) throws InterruptedException {
	 * 
	 * JavascriptFindelements1(); }
	 * 
	 * public static void JavascriptFindelements1() throws InterruptedException {
	 * BaseDriver.driverInstall("EDGE");
	 * driver.get("https://www.amazon.in/ref=nav_logo/"); // find Element by using
	 * JavaScriptExecuter JavascriptExecutor js = (JavascriptExecutor) driver; //
	 * WebElement element = (WebElement)
	 * 
	 * for (int i = 0; i < 1000;) { js.executeScript("scrollBy(0," + i + ")");
	 * 
	 * i = i + 1; }
	 * 
	 * //java script for highlighting
	 * 
	 * js.
	 * executeScript("document.getElementById('email').setAttribute('style','border:4px red solid ; background :yellow');	"
	 * );
	 * 
	 * //java script for sending values
	 * 
	 * js.
	 * executeScript("return document.evaluate(\"//input[@type='text']\",document,null,XPathResult.FIRST_ORDERED_NODE_TYPE,null).singleNodeValue.value='what the Test '"
	 * );
	 * 
	 * //java script for
	 * js.executeScript("document.getElementById('pass').style.background='Yellow';"
	 * ); js.
	 * executeScript("return document.evaluate(\"//input[@type='password']\",document,null,XPathResult.FIRST_ORDERED_NODE_TYPE,null).singleNodeValue.value='what the Test '"
	 * );
	 * 
	 * // element.sendKeys("Password");
	 * 
	 * 
	 * 
	 * //
	 * 
	 * 
	 * Thread.sleep(3000);
	 * 
	 * driver.quit();
	 * 
	 * }
	 */
}
